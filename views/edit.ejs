<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit blog</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
    <h1><%= heading %></h1>
    <% if(locals.blog){%>
        <form id="BlogForm" method="post" action="/blogs/<%=blog.id%>">
            <input type="text" name="title" placeholder="Title"  value = "<%= blog.title %>">
            <textarea name="content" placeholder="content" rows="10"><%= blog.content %></textarea>
            <input type="text" name="author" placeholder="author" value = "<%= blog.author %>">
            <button> <%= submit %> </button>
        </form>
        <% }else{%>
        <form id="newBlogForm" method="post" action="/new/blogs">
            <input type="text" name="title" placeholder="Title" required>
            <textarea name="content" placeholder="content" required rows="10"></textarea>
            <input type="text" name="author" placeholder="author" required>
            <button> <%= submit %> </button>
        </form>
        <%  } %>
    </div>
</body>
</html>